console.log(null > 0);
console.log(null == 0);
console.log(null >= 0);
// above code creats confusion, so avoid this type of comparison

// ===
console.log("2" === 2)  // compare value as well as data type